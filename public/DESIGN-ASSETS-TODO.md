# Design assets pending (Claude Design)

The app chrome/config in this Phase 5 pass references the following raster
image assets that do not exist yet. Please produce and drop them directly
into `public/` at these exact paths and sizes:

- [ ] `public/og.png` — 1200×630 — Open Graph / Twitter share image, referenced
      from `index.html` (`og:image`, `twitter:image`).
- [ ] `public/icon-192.png` — 192×192 — PWA icon, referenced from
      `public/site.webmanifest`.
- [ ] `public/icon-512.png` — 512×512 — PWA icon, referenced from
      `public/site.webmanifest`.
- [ ] `public/apple-touch-icon.png` — 180×180 — iOS home-screen icon,
      referenced from `index.html` (`<link rel="apple-touch-icon">`).

Brand reference: `public/favicon.svg` (purple #863bff), app name
"DutchBuddy", tagline "Nederlands, echt waar." (from
`public/packs/dutch-nl/manifest.json`).

This file can be deleted once all four assets above exist.
